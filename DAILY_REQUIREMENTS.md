# Daily 앱 설계 문서

## 1. 제품 개요

- 앱 이름: Daily
- 목적: 개인이 혼자 사용하는 독립 캘린더 앱
- 핵심 경험: 앱을 실행하면 바로 월간 달력이 뜨고, 날짜 칸 안에서 일정을 플래그 바 형태로 한눈에 확인한다.
- 기존 캘린더 연동: 없음
- 초기 지원 플랫폼: iOS, iPadOS, macOS, Android, Windows
- UI 언어: 한국어만 지원
- 시간대 기준: 한국 시간 기준

## 2. 기술 방향

- 앱 프레임워크: Flutter
- 로컬 데이터베이스: drift + SQLite
- 실시간 동기화: Firebase Auth + Cloud Firestore
- 백업/복원: 앱 로컬 파일 백업/복원, Firestore 원격 데이터 보관
- 알림: 로컬 알림
- AI 일정 해석: 규칙 기반 파서 우선, 복잡한 문장만 Gemini API 선택 사용

## 3. 기본 화면과 UX

- 앱 실행 시 바로 월간 달력 화면을 표시한다.
- Apple Calendar처럼 깔끔한 디자인을 참고하되, 일정이 있는 날을 점으로만 표시하지 않는다.
- 각 날짜 칸에는 일정 제목이 짧은 플래그 바 형태로 표시된다.
- 시간 일정은 `09:00 병원`처럼 표시한다.
- 종일 일정은 제목만 표시한다.
- 모바일에서는 날짜당 최대 3개 일정까지 표시하고, 초과 일정은 `+N` 형태로 표시한다.
- iPad와 macOS에서는 공간이 허용하는 범위 안에서 더 많은 일정을 표시한다.
- 날짜를 터치하거나 클릭하면 해당 날짜의 세부 일정을 보여준다.
- iPhone에서는 날짜 상세를 하단 시트로 표시한다.
- iPad와 macOS에서는 날짜 상세를 우측 패널로 표시한다.
- 채팅 입력창은 항상 화면에 표시한다.
- 빈 날짜를 선택한 뒤 `오후 3시 병원`처럼 입력하면 선택한 날짜 기준으로 해석한다.

## 4. 일정 기능

- 하루 종일 일정을 지원한다.
- 시작 시간만 있는 일정을 허용한다.
- 시작 시간만 입력하면 기본 1시간 일정으로 생성한다.
- 메모 필드를 지원한다.
- 장소 필드를 지원한다.
- 반복 일정을 v1에 포함한다.
- 일정 색상은 AI 자동 분류를 기본으로 한다.
- 사용자가 일정 카테고리와 색상을 직접 수정할 수 있다.
- 삭제한 일정은 최근 삭제 목록 없이 바로 없앤다.

## 5. 반복 일정

v1에서 반복 일정을 지원한다.

필수 반복 유형:

- 매일
- 매주
- 매월
- 매년

반복 일정에 필요한 설정:

- 반복 시작일
- 반복 종료일 없음
- 특정 날짜까지 반복
- 특정 횟수만 반복
- 반복 일정 수정 시 이 일정만 수정
- 반복 일정 수정 시 이후 일정 모두 수정
- 반복 일정 수정 시 전체 반복 일정 수정

## 6. AI 일정 입력

- 기본 자연어 입력은 앱 내부 규칙 기반 파서로 처리한다.
- 다음과 같은 단순 입력은 AI 없이 처리한다.
  - `내일 3시 병원`
  - `5월 20일 오후 2시 회의`
  - `오늘 저녁 7시 약속`
- 복잡하거나 애매한 문장만 Gemini API로 해석한다.
- AI 사용 여부는 설정에서 켜고 끌 수 있다.
- Gemini API 키는 사용자가 설정 화면에 직접 입력한다.
- 민감 키워드가 포함된 경우 AI를 사용하지 않는 옵션을 제공한다.
- AI 결과는 바로 저장하지 않고 확인 카드로 보여준다.
- 필수값이 누락되거나 날짜/시간이 모호하면 저장하지 않고 되묻는다.
- 같은 시간대 일정이 있으면 충돌 경고를 보여준다.
- AI는 일정 카테고리와 색상을 자동 분류한다.

## 7. 알림

- 기본 일정 알림: 일정 시작 1시간 전
- 아침 브리핑 기본 시간: 오전 8시
- 아침 브리핑 시간은 설정에서 변경할 수 있다.
- 일정별로 알림을 끄거나 시간을 수정할 수 있다.
- 일정 수정 시 기존 예약 알림을 갱신한다.
- 일정 삭제 시 기존 예약 알림을 취소한다.
- 아침 브리핑에는 오늘 일정 목록을 요약해서 보여준다.

## 8. 동기화

- 앱은 로컬 SQLite를 우선 원본처럼 사용한다.
- Firebase Auth는 사용자 계정 식별을 담당한다.
- Cloud Firestore는 iOS, iPadOS, macOS, Android, Windows 사이의 실시간 동기화를 담당한다.
- 로컬 저장을 먼저 완료한 뒤 백그라운드에서 Firestore에 반영한다.
- 다른 기기에서 변경된 일정은 Firestore listener를 통해 로컬 DB에 반영한다.
- 충돌이 발생하면 기본적으로 최신 수정 시간을 우선한다.
- Firebase 프로젝트, Firestore Database, 플랫폼별 앱 등록과 설정 파일은 구성 완료 상태다.
- Firebase Auth 이메일/비밀번호 제공자는 Firebase Console에서 수동 활성화가 필요하다.

## 9. 백업/복원

- 앱 문서 폴더 기반 SQLite 파일 백업/복원을 지원한다.
- 자동 백업은 하루 1회 수행한다.
- 수동 백업 버튼을 제공한다.
- 복원 기능을 제공한다.
- 복원 전 현재 데이터를 자동 안전 백업한다.
- 백업 형식은 SQLite 원본 백업을 기본으로 한다.
- 장기 보관과 호환성을 위해 JSON 내보내기도 지원할 수 있게 설계한다.

## 10. 검색

- v1에 검색을 포함한다.
- 제목 검색을 지원한다.
- 메모 검색을 지원한다.
- 장소 검색을 지원한다.
- 카테고리 필터를 지원한다.
- 향후 `치과 언제였지?` 같은 AI 기반 검색으로 확장할 수 있게 설계한다.

## 11. 설정 화면

설정 화면에는 다음 항목을 포함한다.

- 아침 브리핑 시간
- 기본 알림 시간
- AI 사용 여부
- Gemini API 키 입력
- 민감 일정 AI 처리 제한
- 카테고리 색상 관리
- 로컬 파일 백업
- 로컬 파일 복원
- 데이터 내보내기

## 12. 보류 항목

- 앱 잠금: 보류
- TODO 기능: 보류
- 주간 뷰: 보류
- 일간 뷰: 보류
- 기존 Apple/Google/Outlook 캘린더 연동: 보류
- 단축키: 보류
- 공유 캘린더: 보류

## 13. 사용자가 나중에 제공할 항목

- Apple Developer 계정 관련 설정
- Android upload keystore의 별도 안전 백업
- Windows 배포 인증서 또는 설치 패키징 방식
- Firebase Auth 이메일/비밀번호 로그인 활성화
- Gemini API 키

## 14. MVP 성공 기준

프로젝트 첫 버전은 아래 조건을 만족하면 성공으로 본다.

- 앱을 열면 월간 달력이 바로 뜬다.
- 일정이 날짜 칸에 플래그 바로 보인다.
- 날짜를 누르면 해당 날짜의 세부 일정이 보인다.
- 채팅 입력으로 일정을 등록할 수 있다.
- 단순 자연어 일정은 규칙 기반으로 등록할 수 있다.
- 복잡한 자연어 일정은 Gemini API를 통해 해석할 수 있다.
- AI 해석 결과는 저장 전 확인 카드로 보여준다.
- AI가 일정 카테고리와 색상을 자동 분류한다.
- 반복 일정을 만들고 수정할 수 있다.
- 기본 알림 1시간 전이 예약된다.
- 아침 8시 브리핑 알림이 온다.
- iPhone, iPad, macOS, Android, Windows에서 같은 Firebase 계정으로 일정이 동기화된다.
- 로컬 파일 백업과 복원이 가능하다.
- 검색으로 일정을 찾을 수 있다.
